
To run test, ensure phpunit is installed and run one of the following
commands from the root folder:

Drush <7:
phpunit --bootstrap=/path/to/drush/tests/drush_testcase.inc --color

Drush 7+:
phpunit --bootstrap=/path/to/drush/tests/bootstrap.inc --color
